mqtt:
  host: "mqtt"            # MQTT broker host or IP
  port: 1883
  prefix: "OUdrivindrivin" # ir2mqtt topic prefix
  client_id: "ou-bridge"

# Discord webhook for the channel where messages will be posted
discord:
  webhook: "https://discord.com/api/webhooks/REPLACE_ME"

# Runtime flags
runtime:
  post_official: true      # fetch official results after checkered
