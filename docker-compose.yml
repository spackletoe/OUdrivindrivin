version: "3.9"
services:
  iracing-discord-multiprefix:
    build: .
    container_name: iracing-discord-multiprefix
    environment:
      - IRACING_EMAIL=${IRACING_EMAIL}
      - IRACING_PASSWORD=${IRACING_PASSWORD}
    volumes:
      - ./config:/app/config:ro
    restart: unless-stopped
